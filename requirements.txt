# AES-128-CBC for the miIO packet protocol (see hub/miio_protocol.py).
# pycryptodome is chosen for its universal wheels -- no Rust, no cffi, no
# compiler needed on any platform or Python version.
pycryptodome>=3.19

# Testing.
pytest>=7.0
